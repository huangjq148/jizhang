services:
  app:
    build: .
    ports:
      - "8001:8001"
    extra_hosts:
      - "localhost:host-gateway"
    environment:
      - DATABASE_URL=postgresql://postgres:fright2014@localhost:6000/jizhang?schema=public
      - SESSION_COOKIE_NAME=jizhang_session
      - NODE_ENV=production
    restart: unless-stopped
